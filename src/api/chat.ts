import { request } from "@/utils/request";

const chatUrl = ''

// 消息
const  chatConversations = (params: any) => {
  // return request<any>(`${chatUrl}/v1/messages`, "GET", params);
  return {};
}

export { chatConversations };